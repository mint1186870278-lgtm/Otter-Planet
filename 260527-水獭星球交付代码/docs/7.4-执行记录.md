# 7.4 执行记录

记录日期：2026-07-04

## 目标

围绕跑酷段结构治理与部署回归修复，把 7.4 当天所有相关改动集中记录，避免方案、代码、部署说明散落。

参考方案：

```text
docs/跑酷结构治理与回归防护修复方案.md
docs/跑酷部署回归问题修复方案-加载同步与星星布点.md
```

## 当前批次：跑酷结构治理 P0 / P1

状态：已完成并通过验证

### 已完成

```text
1. 审查当前未提交改动，确认主要集中在 loader、App、ParkourScene、SectionParkour、方向教学、星星常量。
2. 确认 public/otterlantis-loader.js 已移除 DOM 猜测 ready 的逻辑，方向正确。
3. 确认 SectionParkour 当前仍混有 loading lifecycle 和 scroll lock，需要先抽成 owner hook。
4. 新增 src/hooks/useParkourLoadingLifecycle.ts：
   - 统一 routeSceneReady / landing / slow / verySlow 状态。
   - 由 ParkourScene 显式 onWarmReady 后调用 markSceneReady。
   - 清理 ready 延迟 timer，避免组件卸载后继续 setState。
5. 新增 src/hooks/useParkourRouteLock.ts：
   - 方向教学期间锁住外层 scroll-snap 和滚动。
   - wheel / touchmove 监听集中 cleanup。
6. SectionParkour.tsx 已接入两个 hook，删除内联 loading timer 和 scroll lock 逻辑。
7. 清理 App.tsx / parkourConstants.ts 编辑遗留的 BOM。
8. 新增 src/components/parkour/starPlacement.ts：
   - 抽出 applyLandStarOverrides，统一基础陆地星 + 调试覆盖坐标的合并逻辑。
   - 抽出 assertLandStarPlacement，检查陆地星数量、重复 id、水面星 id 冲突。
9. 新增 src/components/parkour/starPlacement.test.ts，并加入 npm test。
10. SectionParkour.tsx 和 StarsGroup 已改用 applyLandStarOverrides，避免两边各自 map 一份 active star。
11. starPlacement.ts 新增 isNearAnyStar，统一“某个点是否离星星太近”的距离判断。
12. ParkourScene.tsx 统一生成 activeLandStars：
   - 星星渲染使用 activeLandStars。
   - 障碍物生成避让使用同一份 activeLandStars。
13. Obstacles.tsx 不再直接读 STAR_POSITIONS 做星星避让，改由 ParkourScene 注入 landStars。
14. Scenery.tsx 的树木/灌木路径避让改用 isNearAnyStar helper，距离判断收口到 starPlacement。
15. starPlacement.test.ts 增加 isNearAnyStar 覆盖。
```

### 验证结果

```text
npm run lint：通过
npm test：通过
npm run build：通过
```

构建提醒：

```text
SectionParkour chunk 仍大于 500 kB。
这是既有体积问题，不是本批次新增 hook / starPlacement 引入。
```

### 本批次影响范围

```text
public/otterlantis-loader.js
- 保留：移除 DOM 猜测 ready，loader 只等显式 markSceneReady。

src/hooks/useParkourLoadingLifecycle.ts
- 新增：跑酷加载生命周期 owner。

src/hooks/useParkourRouteLock.ts
- 新增：方向教学期间滚动锁 owner。

src/components/SectionParkour.tsx
- 接入 loading lifecycle hook。
- 接入 route lock hook。
- 接入统一 active land stars 合并逻辑。

src/components/ParkourScene.tsx
- 保留 onWarmReady / SceneReadyProbe，R3F 渲染 2 帧后显式上报。
- 统一计算 activeLandStars，并传给星星渲染和障碍物避让。

src/components/DirectionTutorialOverlay.tsx
- 保留 pointer stopPropagation，避免方向按钮事件冒泡影响外层滚动。

src/components/parkour/Stars.tsx
- 接入统一 active land stars 合并逻辑。

src/components/parkour/Obstacles.tsx
- 障碍物生成避让改用 ParkourScene 注入的 activeLandStars。
- 以后调试面板移动星星后，障碍物避让会跟着实际星星位置走。

src/components/parkour/Scenery.tsx
- 树木/灌木静态散布避让改用 starPlacement 的 isNearAnyStar。
- 因树木散布是模块级静态生成，当前仍基于基础 STAR_POSITIONS，不读取运行时 debug 覆盖。

src/components/parkour/starPlacement.ts
- 新增 isNearAnyStar，统一距离避让判断。

src/components/parkourConstants.ts
- 当前保留 21 颗陆地星，进度仍由 SectionParkour 封顶 9。

package.json
- npm test 增加 starPlacement 测试。
```

### 仍待后续处理

```text
1. SectionParkour 仍然偏大，下一批应继续拆 useControlTutorialFlow / useParkourInput。
2. 尚未做 Playwright 网络断言：
   - 教学阶段不请求 scene-terrain-opt.glb
   - loader 只在 markSceneReady 后消失
3. 已把部署快照从 docs/deploy-20260704-terrain-delay 迁到 deploy/packages/20260704-terrain-delay。
4. 树木散布是模块级静态生成，暂不响应 debugLandStars。正式坐标写回 STAR_POSITIONS 后会自然生效。
```

## 当前批次：文档 / 部署快照结构整理

状态：已完成

### 已完成

```text
1. 复制 docs/deploy-20260704-terrain-delay 到 deploy/packages/20260704-terrain-delay。
2. 确认目标目录包含：
   - README.md
   - verify-online-terrain-delay.sh
   - package/index.html
   - package/assets/index-DzNmtrFH.js
   - package/assets/SectionParkour--w0vNVV1.js
3. 删除 docs/deploy-20260704-terrain-delay 源目录。
4. 更新 docs/跑酷结构治理与回归防护修复方案.md 中的发布包引用路径。
5. 更新 .gitignore：
   - deploy/ 仍默认忽略，避免 zip 和临时包进库。
   - 仅放开 deploy/packages/20260704-terrain-delay，保证本次从 docs 移出的发布快照不会丢。
```

### 说明

```text
docs/ 继续作为人读方案和记录区。
deploy/packages/ 作为发布快照和可上传包存放区。
```

## 当前批次：合并 PR #2 前端流程修复

状态：已本地合并，未推送远端

### PR 状态

```text
PR #1：Delay full terrain loading until after tutorial
状态：MERGED

PR #2：[codex] Harden frontend flow against hidden section conflicts
状态：OPEN / DRAFT
分支：codex/terrain-delay-load
本地 merge commit：78c2863
```

### 已完成

```text
1. 执行 git fetch --all --prune，确认远端 codex/terrain-delay-load 比 main 多 3 个 commit。
2. 使用 gh pr view 查看 PR #2：
   - GitHub 标记 mergeable = MERGEABLE
   - 但 PR 仍是 Draft，尚未在 GitHub merge
3. 本地当前 7.4 未提交改动已保存到 stash：
   - stash@{0}: pre-pr2-merge-local-7.4-work
4. 工作树恢复到 HEAD 后，本地合并 origin/codex/terrain-delay-load。
5. 合并策略按“优先用他的东西”处理：
   - PR #2 的 App / SectionParkour / sectionFlow / smoke-flow 等改动作为主线保留。
   - 未把 stash 里的重叠代码重新覆盖回来。
6. 只从 stash 恢复文档记录：
   - docs/7.4-执行记录.md
   - docs/跑酷结构治理与回归防护修复方案.md
```

### 验证结果

```text
npm run lint：通过
npm test：通过
npm run build：通过
```

未通过项：

```text
npm run smoke:flow：未能在当前本机环境运行
原因 1：默认截图目录 /tmp 映射到不可写的 E:\tmp。
原因 2：改用项目内截图目录后，当前 Node 环境缺少 global WebSocket。
判断：这是本地 smoke 运行环境限制，不是 lint/test/build 失败。
```

### 当前注意

```text
1. 本地 main 现在 ahead origin/main 4 个 commit。
2. PR #2 在 GitHub 仍是 Draft；本地已经合并，但尚未 push 到 origin/main。
3. stash@{0} 仍保留，里面有合并前本地 7.4 代码和文档备份。
4. 如果后续要继续“优先他的东西”，不要直接 git stash pop；应只按文件/片段挑回文档或明确不冲突内容。
```
