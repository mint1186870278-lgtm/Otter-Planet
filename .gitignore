# ── Otter-Planet 仓库根 ──────────────────────────────────────────────
# 约定：仓库根只放「按日期命名的版本大文件夹」+ 本说明，方便区分版本。
# 策略 = 默认忽略根目录下的一切，只显式放行版本文件夹和索引文件。

/*
!/.gitignore
!/README.md
!/260527-水獭星球交付代码/

# ── 版本文件夹内部的补充忽略 ──────────────────────────────────────────
# 注：260527 文件夹自带 .gitignore 已排除 node_modules/ dist/ .env* *.log
#     *.blend _archive-large-source/。这里补充它未覆盖的临时/大体积产物：
260527-水獭星球交付代码/dist-main-tmp/

# 交付 zip / 上传包等大压缩产物一律不入库（用干净快照区分版本，不靠 zip）
**/*.zip

# 密钥兜底（防止任何 .env 泄漏；example 模板保留）
**/.env
**/.env.*
!**/.env.example

# 杂项
**/.DS_Store
**/*.log
